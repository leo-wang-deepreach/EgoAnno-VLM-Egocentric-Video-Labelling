You are the FINAL GATE for a robot-learning annotation pipeline. The frames below sample the ENTIRE clip in chronological order (top-right clock = time authority). You see the whole video at once, so you do exactly TWO jobs: (B) spot-check ONLY the flagged segments against their frames, and (C) merge artificial window-edge cuts. Direction has already been decided by a dedicated upstream stage — you do NOT re-derive it; you only confirm whether the timeline's labels are consistent with it.

Apply the named disciplines from your system prompt (ANTI-MIRROR, MIXED-IS-TERMINAL, TRACK-GROUNDED ROLE, BURST-DECIDES-ROLE, STILL-NOT-HOLD, COMPLETE-THE-ACTION) — they are NOT restated here.

Decided direction (from the direction stage; treat as settled unless a flagged segment's frames flatly contradict it): <<DIRECTION>>
Purpose hypothesis: <<GOAL>>
Possession track (independent ground truth): <<TRACK>>
Reduced rotation/role/color verdicts (decisive bursts only): <<BURSTS>>
DETECTED TRANSITIONS (a dense frame scan found these brief place/pickup/throw/handoff events; they are the most reliable boundary signal in the run — every one should line up with a segment boundary, and none should be missing): <<TRANSITIONS>>

FINAL TIMELINE (numbered; each segment is a block — "L:" = LEFT hand, "R:" = RIGHT hand, on their own lines):
<<TIMELINE>>

SEGMENTS PRE-FLAGGED FOR REVIEW (these are the ONLY labels you scrutinize — a neighbor-consistency reviewer and deterministic scans judged each suspicious: an object/part/color contradicting the surrounding cycle, a hand-role swap, an action breaking the local flow, a monotone run hiding a missed pick, or a track/label mismatch). Give EACH extra frame-level scrutiny; correct it if the frames disagree; leave it unchanged if it is right:
<<FLAGGED>>

<<ANALYSIS>>
The analysis block above (if present) is produced by CODE, not a model — plain counts over the track/labels/transitions. It is ADVISORY: use it to decide WHERE to look harder, but it can be wrong at edges (a clip starting/ending mid-cycle leaves a pick/place unmatched; near-identical wording is sometimes two real actions). Confirm every analysis hint against the frames before acting on it; never change a label solely because a count suggested it.

=== JOB B — SPOT-CHECK FLAGGED SEGMENTS ONLY ===
Do NOT re-label segments that are not flagged. For each flagged segment, in clock order:
1. Read its own frames plus the 1-before/1-after neighbor.
2. Check the forearm assignment against the TRACK (TRACK-GROUNDED ROLE) and any role burst (BURST-DECIDES-ROLE). Swap L/R if the track/burst contradicts the label.
3. Check hold vs N/A (STILL-NOT-HOLD): a hold needs a visibly gripped object; an empty motionless hand is N/A.
4. Check the action is not mirrored (ANTI-MIRROR) and the colour is not locked or over-specified (MIXED-IS-TERMINAL; use the colour burst as ground truth).
5. Check the verb names a single visible action with its endpoint (COMPLETE-THE-ACTION). A label that bundles two actions (e.g. "drop part take new unit") is invalid — replace it with the ONE action its frames actually show; if its frames show two distinct visible actions, do not jam them into one label — instead emit a "split_request" for that segment so an upstream split runs, and leave the existing label.
6. MISSED ACQUISITION (report only, do not relabel): if a flagged deposit is preceded by manipulation of a NEW object with no acquisition between them, note it in findings so the deterministic gate routes a split — do not invent a pick label.
7. DETECTED TRANSITION = REAL ACTION (relabel, don't dismiss): a place/pickup/hand-off reported by the dense frame scan (it appears in the analysis block / DETECTED TRANSITIONS, e.g. "place at 13.6s") is a real action change unless the frames CLEARLY show the grip never opened. Do NOT wave it away as a "momentary regrasp," and do NOT merge across it. If a segment spans such a transition but its label says hold/twist/thread, CORRECT it: the acting hand (whose grip opens to place, or closes to pick) gets "place the <object>" / "pick up the <object>"; the other hand becomes N/A. NAME THE WHOLE OBJECT that leaves/enters the hand: if parts have been joined, it is the assembled UNIT ("place the bolt-nut unit"), NOT a loose sub-part ("place the nut") — a "place the nut" label sitting inside a continuous threading run is the mislabel to FIX (relabel it to the unit), never an excuse to merge the segment away. Put the action on the hand that actually changed — never label the placing hand "hold".

A correction is a FULL replacement label obeying the system-prompt LABEL FORMAT, or exactly "N/A". Only correct labels the frames contradict; never restyle a consistent label.

=== JOB C — WINDOW-SEAM MERGES ONLY ===
The edge verifier already snapped or merged every within-cycle boundary at 30fps, so do NOT re-litigate those. Your scope is the SEAMS where two fixed processing windows met — a boundary that splits ONE continuous sub-goal only because the clip was chunked there. List such a boundary time in "merge_at_sec". Only a time lying exactly between two adjacent segments. NEVER merge across a possession change (object enters/leaves a hand, or a hand-to-hand transfer) or a real purpose change — those boundaries are real. Do not merge a long identical-label run into one giant span; if labels repeat across many cycles, that is a missed-pick signal for findings, not a merge.

=== PURPOSE ===
Give the corrected one-sentence purpose in "purpose_check"; set "purpose_verdict" to "confirmed" if it matches the frames, "corrected" otherwise.

DIRECTION: report the direction you actually SEE in the frames in "direction" (assembly / disassembly / transfer_or_portioning / mixed_or_alternating / other_or_ambiguous). If it matches <<DIRECTION>>, just echo it. But if the frames CLEARLY show the opposite (e.g. parts are being SEPARATED/unscrewed when the decided direction says assembly), set "direction" to the correct one — the whole timeline will be relabelled to match. Only override on clear visual evidence, not a single ambiguous frame.

If everything is consistent, return empty corrections and merge lists.

=== OVERALL QUALITY VERDICT — STRICT GATE: PROVE IT IS RIGHT, OR RERUN ===
This trains a robot to REPRODUCE the human's actions. The DEFAULT verdict is "needs_rerun". Set "good" ONLY after you have WALKED THE CLIP action by action against the frames and AFFIRMATIVELY confirmed EVERY check below. If you cannot confirm even one, or you are unsure, set "needs_rerun". A passing timeline is achievable and not rare — but it must EARN the pass; do not grant it out of politeness, fatigue, or because the timeline merely looks internally tidy.

PASS REQUIRES ALL OF (verify each against the frames, not against the timeline's own neatness):
  A. COVERAGE: every DETECTED TRANSITION lines up with a segment boundary (±~0.4s), AND no place/pickup/throw you can SEE in the frames lacks a boundary.
  B. ATOMIC TRANSITION SPANS: in EACH place/pickup/throw span, exactly the ACTING hand carries the action and the OTHER hand is N/A. Both hands active through a transition = FAIL.
  C. NO FROZEN ROLE: no hand carries the SAME label straight across a detected transition. A hand labeled "hold the <X>" on both sides of a place/pickup that <X> went through is the inertia-hold error = FAIL. Roles may swap cycle to cycle; a hand frozen on one label across the whole clip while transitions happen = FAIL.
  D. NO IDENTICAL NEIGHBORS ACROSS A TRANSITION: two adjacent segments with IDENTICAL left+right labels that have a detected transition between them = FAIL (relabel each side by its own action — this is a label error the merger cannot fix, NOT a merge candidate).
  E. FRAME-TRUE OBJECT IDENTITY: every label matches the frames; the object that leaves/enters a hand is named as what it IS (the assembled UNIT, or "another <unit>" for a fresh one from a pile — never a loose sub-part like "the nut" when it is on the bolt). A re-grasp mislabeled "pick up the nut" = FAIL.
  F. DIRECTION MATCHES END-STATE: the labels' direction agrees with what the frames show ACCUMULATING by the end (more joined units = assembly; more separated loose parts = disassembly). If the timeline says assembly but the end-state shows parts being SEPARATED (or vice-versa), the direction is wrong = FAIL (report the corrected direction in "direction").

DO NOT FAIL FOR: pure over-splitting (one continuous action chopped into several identical-label slivers with NO transition between them — a delete-only merge critic fixes that after you). Over-segmentation alone is not a rerun reason; A–F are.

The CODE-GENERATED ANALYSIS above may flag "frozen_across_transition?" or "dup_across_transition?" — those map directly to checks C and D. Confirm each against the frames; if real, it is an automatic FAIL.

In "rerun_feedback" give the NEXT attempt a DETAILED, ACTIONABLE PUNCH-LIST — one line per defect, each naming the segment number AND time, which check (A–F) it violates, and the EXACT fix (which hand gets which label, or where to split). List EVERY defect you found, not just one; be specific enough that the next labeler can apply each item verbatim. Example: "#3 (~12.8s) check C: LEFT is 'hold the silver bolt' across the place — set LEFT='place the bolt-nut unit', RIGHT=N/A. #4 (~14s) check E: RIGHT 'pick up the nut' is a fresh unit from the pile — set RIGHT='pick up another bolt-nut unit', LEFT=N/A. #6/#7 check D: identical labels across the pickup at 28.8s — relabel #7 RIGHT='pick up another bolt-nut unit'. Direction OK." Use "" only when you set "good".
You may also REQUEST a specific check on demand: set "request_merge_critic": true to have the delete-only merge critic re-run, or "request_completeness": true to re-run the pick/place completeness audit, if you suspect over-segmentation or a missing pick/place chain you can't pin down yourself.

Raw JSON only:
{"purpose_check": "<one sentence>", "purpose_verdict": "confirmed"|"corrected", "direction": "assembly"|"disassembly"|"transfer_or_portioning"|"mixed_or_alternating"|"other_or_ambiguous", "findings": "<one short paragraph: which flagged segments were corrected and why, any missed acquisition noted, whether labels were consistent with the decided direction>", "merge_at_sec": [<number>, ...], "split_request": [<seg#>, ...], "corrections": [{"seg": <1-based number>, "hand": "left"|"right", "label": "<replacement>"}], "quality_verdict": "good"|"needs_rerun", "rerun_feedback": "<short concrete fix instructions, or empty>", "request_merge_critic": true|false, "request_completeness": true|false}
