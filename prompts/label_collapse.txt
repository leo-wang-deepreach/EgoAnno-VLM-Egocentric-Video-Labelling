You produce the FULL per-hand action timeline for this clip in ONE pass, for robot learning. Apply the named disciplines from your system prompt (FOREARM L/R, ACTING vs SUPPORTING, ANTI-MIRROR, TRACK-GROUNDED ROLE, MIXED-IS-TERMINAL, STILL-NOT-HOLD, COMPLETE-THE-ACTION).

You watch the WHOLE clip from 0 to <<B>>s at 10 fps (top-right clock = absolute seconds, the time authority). You are also given RELIABLE pre-computed facts — trust them as your primary ground, use the frames to name the visible motion:

Decided direction (settled): <<DIRECTION>>
Object catalogue (use these identities, spelled the same way every time): <<INVENTORY>>
(No task-goal narrative is given on purpose — you READ each action from the frames + facts below. Do NOT assume a fixed count or structure, e.g. "many X onto one Y"; every cycle may act on a DIFFERENT unit.)

RELIABLE per-hand possession over time (the 1A ground truth — which object each forearm holds, or "empty"; this is your authority for WHO holds WHAT and WHO is idle, per hand — never swap L/R):
<<CONTACT>>

DETECTED TRANSITIONS (a dense frame scan — strong evidence of the brief boundary events; honor them):
<<TRANSITIONS>>

REQUIRED CUTS — you MUST place a segment boundary at EACH of these times, and they are HAND-TAGGED «KIND-HAND» (e.g. «PLACE-LEFT» = the LEFT hand places there, «PICKUP-RIGHT» = the RIGHT hand picks up there). A real place/pickup/throw happens at each; it can never be inside a segment: <<REQUIRED_CUTS>>

Decisive rotation/role/colour bursts (reliable for those moments):
<<BURSTS>>

=== PRIOR-ATTEMPT FEEDBACK (a STRICT verifier reviewed the last attempt — APPLY every item) ===
This is a punch-list of exactly what was wrong last time. Fix each one in this attempt; do not reproduce the same errors. If it says a hand was frozen "hold" across a place/pickup, put the action on the acting hand and N/A the other; if it says a re-grasp was mislabeled, name the fresh unit ("another <unit>"). If it is "(first attempt …)", there is no prior feedback yet.
<<FEEDBACK>>

=== YOUR JOB: COLLAPSE THE FACTS INTO AN ACTION TIMELINE ===
Build one continuous, non-overlapping per-hand timeline covering 0 to <<B>>s.
- CUT a new segment exactly where an action CHANGES: a hand starts/stops gripping (per the possession facts), a detected place/pickup/throw/handoff, a new object enters a hand, or the motion itself changes (e.g. threading -> placing). The possession facts + transitions tell you where these boundaries are.
- COLLAPSE: while both hands keep doing the SAME thing to the SAME object, it is ONE segment, however long — do not chop a steady action into slivers.
- LABEL each hand for each segment: a 2-8 word sentence, ONE action head, "<verb> the <object> [short modifier]"; or exactly "N/A" if the possession facts show that hand empty/out-of-frame across the segment.

=== LABEL THE CHANGE, NOT THE BACKGROUND ===
- A grip that OPENS = a place ("place the <object>") or, if a quick toss/flick/fling, "throw the <object>" — never "hold". A grip that CLOSES on a resting object = "pick up the <object>". Put the action on the hand whose grip actually changed (per the facts); never pin it on the wrong hand.
- "hold"/"steady"/"brace" only for a hand gripping ONE object and keeping it still the whole segment with no release and no manipulation.
- Judge each segment's action from its MIDDLE; the edges are reference.
- N/A is per-hand and grounded in the possession facts: a hand the facts show gripping is NEVER N/A (at least "hold the <object>"); a hand the facts show empty is N/A. Do not put N/A on a gripping hand.
- TWO PARTS IN A JOIN: the hands usually hold DIFFERENT parts — one steadies the base, the other turns/threads/places the other part. Name each hand's actual part distinctly.
- Vague verbs BANNED (manipulate/handle/work/adjust/use). Verbs consistent with the decided direction.

=== AT A PLACE / PICKUP / THROW (each REQUIRED CUT) — ATOMIC, HAND FROM THE TAG, NAME THE WHOLE OBJECT ===
A place/pickup/throw is a ONE-HAND event. The «KIND-HAND» tag tells you WHICH hand acts: on a «PLACE-LEFT»/«PLACE-RIGHT» span the named hand carries "place the <object>"; on a «PICKUP-LEFT»/«PICKUP-RIGHT» span the named hand carries "pick up the <object>". The OTHER hand is "N/A" — idle while the one hand acts. Do NOT give both hands active labels through a transition, and do NOT label the acting hand "hold" — its grip is opening (place) or closing (pickup).
- ROLES SWAP CYCLE TO CYCLE — DO NOT FREEZE ONE HAND. The hand that places in one cycle may be the hand that picks in the next. Read the tag each time; never carry "hold the bolt" on one hand straight across a place/pickup the OTHER hand performed.
- NAME THE OBJECT THAT ACTUALLY LEAVES OR ENTERS THE HAND, not the sub-part being turned. If two parts have been joined (e.g. a nut threaded onto a bolt), the hand sets down or picks up the ASSEMBLED UNIT — label it the unit ("place the bolt-nut unit"), NEVER "place the nut" while the nut is on the bolt (that reads as a separate nut placement and is wrong). When parts are being SEPARATED, name the piece that comes free.
- A PICKUP AFTER A PLACE IS A NEW INSTANCE. When a hand picks up a unit after one was just placed/set aside, it is "pick up another <unit>" (a different unit from a pile/surface), NOT "pick up the nut" and NOT the same unit continued. The task is often work-a-unit → set it down → pick up the NEXT unit → work that one.
- UNIT NAME APPEARS AT THE JOIN, NOT BEFORE: single-part names while parts are still separate; switch to the combined "unit" name only at/after the place that follows their joining — never let "unit" drift back into the earlier threading segments.

Raw JSON only:
{"think": "<2-3 sentences: the repeated structure you see and where the boundaries are>", "segments": [{"start_sec": <n>, "end_sec": <n>, "left": "<phrase or N/A>", "right": "<phrase or N/A>"}]}
