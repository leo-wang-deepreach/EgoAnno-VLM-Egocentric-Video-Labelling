You label BOTH hands for ONE segment of an egocentric two-handed manipulation clip, for robot learning. Apply the named disciplines from your system prompt (FOREARM L/R, ACTING vs SUPPORTING, ANTI-MIRROR, TRACK-GROUNDED ROLE, BURST-DECIDES-ROLE, MIXED-IS-TERMINAL, STILL-NOT-HOLD, COMPLETE-THE-ACTION) — they are not restated here.

=== WHAT YOU SEE ===
You watch this clip from t=<<OA>>s to t=<<OB>>s at 10 fps (top-right clock = absolute seconds, the time authority). The CORE segment you must label is t=<<A>>s to t=<<B>>s. The moments before <<A>> and after <<B>> are CONTEXT ONLY — use them to tell whether the core action is a fresh start or the continuation/tail of a neighbor, but DO NOT label them. Your labels describe ONLY what happens in the core [<<A>>, <<B>>]. Apply FOREARM L/R (left-edge forearm = left hand) every frame.

Task goal (derived upstream, a hypothesis): <<GOAL>>
Decided direction (settled upstream — your labels must be consistent with it): <<DIRECTION>>
Objects: <<INVENTORY>>
Possession track over this window — RELIABLE. It is your PRIMARY evidence for which forearm holds what and which is empty over time. Ground your reading in it; override only if the frames plainly contradict it: <<TRACK>>
Per-frame contact facts inside the core — a RELIABLE per-0.1s record of which object each hand holds (the 1A ground truth). Your labels MUST be consistent with it: a hand it shows EMPTY across the core is N/A; a hand it shows GRIPPING an object is doing something with THAT named object. Use these facts for WHO holds WHAT and WHO is empty (per hand, do not swap L/R); use the frames to name the visible ACTION:
<<CONTACT_FACTS>>
Decisive rotation/role/colour verdicts inside the core (these ARE reliable for those moments): <<BURSTS>>
Previous segment's label (continuity, TEXT only): L: <<PREV_LEFT>> | R: <<PREV_RIGHT>>
Next segment's label (continuity, TEXT only):     L: <<NEXT_LEFT>> | R: <<NEXT_RIGHT>>

=== HOW TO USE THE CONTACT FACTS + OVERLAP ===
- The contact facts say WHAT each hand holds; you name WHAT IT DOES with it. A hand the facts show as "empty" across the core cannot be given a holding/manipulating verb — it is N/A (or a reach, if a grasp visibly begins).
- If the core's motion is the SAME continuous action as the previous label and the contact facts show no possession change at <<A>>, you are mid-action — name the ongoing action, do not invent a fresh "pick".
- If a hand GAINS an object at the core's start (ctx-before frames show it empty, then holding), name the pick and its source. If a hand RELEASES into a destination near the core's end, name the place with its destination.

=== STEPS ===
1. LEFT hand only across the core frames (use ctx frames + contact facts to settle the edges).
2. RIGHT hand only across the core frames.
3. RECONCILE (a transfer needs a giver AND a receiver; one hand may support what the other works).
4. Run the named checks the system prompt defines.

=== LABEL THE CHANGE, NOT THE BACKGROUND ACTION (most important) ===
This segment was cut HERE because something changed. Name what each hand actually DOES in this span — the brief defining action WINS over the steady background motion:
- If a hand RELEASES / throws / drops / sets down / lets go of an object anywhere in this span, that hand's label is a PLACE or RELEASE ("place the <object>", "drop the <object>") — NEVER "hold", and never the ongoing "twist/thread". A grip that opens is a place, full stop.
- If a hand CLOSES on a new object (empty, then gripping), that hand's label is "pick up the <object>" — not "hold".
- Identify WHICH hand changed: the hand whose grip OPENS or CLOSES is the actor of the place/pick — put the action on THAT hand. The other hand gets its own real action or N/A. Never pin the change on the wrong hand (e.g. labelling the placing hand "hold" and the steady hand "pick up").
- "hold"/"steady"/"brace" is allowed ONLY for a hand that grips ONE object and keeps it still for the WHOLE span with NO release and NO manipulation. If anything happens to that object, name that instead — "hold" is never the answer when an action occurs.
- If a DETECTED TRANSITION is supplied below, it is strong evidence a place/pickup/throw happens here — your label MUST reflect it, overriding any "hold"/"twist". A quick toss/flick release (object cast away, not set down) is "throw the <object>", not "place".
- JUDGE FROM THE MIDDLE: the action in the MIDDLE of the core [<<A>>, <<B>>] is the truth — name THAT. The first and last frames are reference only (they may show the tail of the previous action or the start of the next); do not let an edge frame decide the label.
- N/A IS PER-HAND AND TRACK-GROUNDED: a hand is N/A ONLY when the contact facts show THAT hand empty/out-of-frame. A hand the facts show GRIPPING an object is NEVER N/A — at minimum "hold the <object>". When one hand does a one-handed place/pickup, the OTHER hand is N/A ONLY IF the facts show it empty too; if the other hand is still gripping/steadying, label its hold. Never put N/A on the gripping hand — check WHICH hand the facts show as empty and N/A exactly that one.
- TWO PARTS IN A JOIN: when the hands work a join, they usually hold DIFFERENT parts — one steadies the base part, the other turns/threads/places the OTHER part onto it. Name each hand's ACTUAL part distinctly (the turned/placed part vs the held base), even if similar in colour/material; do not collapse both to one name.

Each non-N/A label: 2-8 words, ONE action head, "<verb> the <object> [short modifier]". No "then/while/and", no pronouns, never "left hand"/"right hand" inside a label.

NAME THE SPECIFIC ACTION — vague verbs are BANNED. Never use "manipulate", "handle", "work", "interact with", "use", "adjust", or "do something to". State the actual visible motion: hold/steady/brace, thread/screw/twist/turn/tighten/loosen/unscrew, insert/seat, pick up/grab/lift, place/set down/drop/release, push/pull, hand off. The verb must be consistent with the decided direction (assembly → joining verbs like thread/screw-on/insert; disassembly → separating verbs like unscrew/remove/pull-out). If you genuinely cannot tell which motion it is, pick the most specific one the frames support — never fall back to a vague verb.

N/A IS EXPECTED, not a fallback: if a hand is idle this segment — empty, resting, waiting, or out of frame (the in-window contact facts show it empty, or the other hand is doing a one-handed action like a place or pickup) — its label is exactly "N/A". Do NOT manufacture a "hold" for an idle hand. A one-handed place or pickup means the other hand is N/A.

OBJECT NAMES: use ONLY the catalogue identities in Objects above, spelled the same way every segment. When parts are joined, name the one combined unit; do not call the combined unit by a single part's name, and do not rename the same object between segments.

Raw JSON only:
{"think": "<2-3 sentences: per-hand reading and how the contact facts / overlap settled the edges>", "left": "<phrase or N/A>", "right": "<phrase or N/A>"}
